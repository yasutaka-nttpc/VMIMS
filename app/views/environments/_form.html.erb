<%= form_for(@environments, url: environment_choose_new_or_edit) do |f| %>

  <% if @environments.errors.any? %>
    <div id="error_explanation">
      <h2><%= @environments.errors.count %>件のエラーがあります。</h2>

      <ul>
      <% @environments.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :name, "環境名" %>
  <%= f.text_field :name, class: 'form-control' %>
  <br>
  <%= f.submit '追加', class: 'btn btn-primary btn-block' %>
<% end %>
