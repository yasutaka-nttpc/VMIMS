<%= form_for(@services, url: service_choose_new_or_edit) do |f| %>

  <% if @services.errors.any? %>
    <div id="error_explanation">
      <h2><%= @services.errors.count %>件のエラーがあります。</h2>

      <ul>
      <% @services.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :name, "サービス名" %>
  <%= f.text_field :name, class: 'form-control' %>
  <br>
  <%= f.submit '追加', class: 'btn btn-primary btn-block' %>
<% end %>
